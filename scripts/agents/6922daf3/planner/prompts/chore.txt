/chore 6922daf3 Add auto-crop post-processing to try-on images to ensure model fills the frame. In apps/web/app/api/generate-image/route.ts, after the background removal step (line 425-434), add a new post-processing step that: 1) Creates a Python script scripts/image_processing/auto_crop_subject.py that uses PIL/Pillow to detect the bounding box of non-transparent pixels in a PNG image and crops to that bounding box with 3% padding on all sides, then resizes to maintain aspect ratio 2) Calls this script after removeBackgroundFromFile() for fitting-model, try-on, and try-on-dual generation types 3) The script should take input_path and output_path as arguments, similar to remove_bg_rembg.py 4) Add error handling that falls back to the uncropped image if cropping fails. This ensures the model fills 90%+ of the frame regardless of what the AI generates.